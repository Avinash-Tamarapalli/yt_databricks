resources:
  pipelines:
    pipeline_earthquake_details_bronze:
      name: earthquake_details_bronze
      libraries:
        - glob:
            include: ../src/earthquake_details_bronze/transformations/**
      schema: ${var.bronze_schema}
      continuous: false
      development: false
      photon: true
      channel: CURRENT
      catalog: ${var.catalog}
      serverless: true
      root_path: ../src/earthquake_details_bronze
