resources:
  pipelines:
    earthquake_weekly_summary_bronze:
      name: earthquake_weekly_summary_bronze
      catalog: ${var.catalog}
      schema: bronze_dev

      serverless: true
      photon: false

      development: false
      continuous: false

      channel: CURRENT

      libraries:
        - file:
            path: ../src/earthquake_weekly_summary_bronze/transformations/earthquake_weekly_summary_bronze.py